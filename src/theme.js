import { createTheme } from "@mui/material";
export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1d2c94",
    },
    secondary: {
      main: "#318a34",
      light: "#A1E2A4",
    },
  },
});
